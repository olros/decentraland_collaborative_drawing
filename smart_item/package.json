{
  "name": "eit-2023-project",
  "version": "1.0.0",
  "description": "EiT 2023 project for collaborative drawing in Decentraland",
  "scripts": {
    "start": "dcl start",
    "dcl-pack": "dcl pack",
    "build": "build-ecs",
    "watch": "build-ecs --watch",
    "deploy:now": "dcl export && now export",
    "ecs:install": "yarn add -D decentraland-ecs@latest",
    "ecs:install-next": "yarn add -D decentraland-ecs@next"
  },
  "devDependencies": {
    "decentraland-builder-scripts": "^0.24.0",
    "decentraland-ecs": "^6.11.11"
  }
}
